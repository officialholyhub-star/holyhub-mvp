import { requireRole } from "@/lib/auth/require-user";
import type { Settings } from "@/lib/marketplace";
import { MarketNav } from "@/components/market-nav";
import { Notices } from "@/components/notices";
import { SubmitButton } from "@/components/submit-button";
import { saveSettings } from "./actions";
export default async function AdminSettings({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const {supabase}=await requireRole("admin");const {data,error}=await supabase.from("platform_settings").select("*").single();if(error)throw new Error("Settings unavailable");const s=data as Settings;return <section className="content-narrow"><MarketNav area="admin"/><div className="section-heading"><p className="eyebrow">Set the rules, clearly</p><h1 className="page-title">Marketplace settings.</h1><p>The starting prices are 10 free product listings, £0.20 per additional listing and 5% commission. Confirm any policy changes with Alea.</p></div><Notices {...await searchParams}/><form action={saveSettings} className="form"><section className="card form"><h2>Fees & commission</h2><div className="form-columns"><div className="field"><label htmlFor="commission">Sales commission (%)</label><input id="commission" name="commission" inputMode="decimal" defaultValue={s.commission_bps/100} required/></div><div className="field"><label htmlFor="free_listing_limit">Free listings per seller</label><input id="free_listing_limit" name="free_listing_limit" type="number" min={0} max={10000} defaultValue={s.free_listing_limit} required/></div></div><div className="form-columns"><div className="field"><label htmlFor="listing_fee">Additional listing fee (£)</label><input id="listing_fee" name="listing_fee" inputMode="decimal" defaultValue={(s.listing_fee_pence/100).toFixed(2)} required/></div><div className="field"><label htmlFor="listing_allowance_mode">Free allowance applies to</label><select id="listing_allowance_mode" name="listing_allowance_mode" defaultValue={s.listing_allowance_mode??""}><option value="">Not yet confirmed</option><option value="lifetime">First listings ever created</option><option value="active">Currently published listings</option></select></div></div></section><section className="card form"><h2>Seller reserves</h2><p>Leave unconfirmed policies blank. Suggested ranges in the build specification are guidance, not active financial settings.</p>{(["new","established","high_risk"] as const).map(tier=><div key={tier}><h3>{tier==="new"?"New sellers":tier==="established"?"Established sellers":"Higher-risk sellers"}</h3><div className="form-columns"><div className="field"><label htmlFor={`${tier}_reserve`}>Reserve (%)</label><input id={`${tier}_reserve`} name={`${tier}_reserve`} inputMode="decimal" defaultValue={s[`${tier}_reserve_bps`]==null?"":s[`${tier}_reserve_bps`]!/100}/></div><div className="field"><label htmlFor={`${tier}_reserve_days`}>Hold for (days)</label><input id={`${tier}_reserve_days`} name={`${tier}_reserve_days`} type="number" min={0} max={365} defaultValue={s[`${tier}_reserve_days`]??""}/></div></div></div>)}</section><section className="card form"><h2>Appeals</h2><p>Both values must be configured before new decisions offer an in-app appeal. Confirm these with Alea; no appeal deadline has been invented.</p><div className="form-columns"><div className="field"><label htmlFor="appeal_days">Days to appeal a decision</label><input id="appeal_days" name="appeal_days" type="number" min={1} max={365} defaultValue={s.appeal_days??""}/></div><div className="field"><label htmlFor="appeal_limit">Maximum appeals per case</label><input id="appeal_limit" name="appeal_limit" type="number" min={1} max={10} defaultValue={s.appeal_limit??""}/></div></div></section><section className="card form"><h2>Delivery & tax decisions</h2><p>These notes record the agreed policy. They are not a shipping-rate or tax engine, and saving them does not switch checkout on.</p><div className="field"><label htmlFor="shipping_policy">Shipping policy</label><textarea id="shipping_policy" name="shipping_policy" rows={4} maxLength={2000} defaultValue={s.shipping_policy??""}/></div><div className="field"><label htmlFor="tax_policy">Tax treatment</label><textarea id="tax_policy" name="tax_policy" rows={4} maxLength={2000} defaultValue={s.tax_policy??""}/></div></section><SubmitButton pendingText="Saving…">Save confirmed settings</SubmitButton></form></section>;}
